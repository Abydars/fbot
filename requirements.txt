# Core async framework
fastapi
uvicorn[standard]
websockets

# Data / indicators
pandas
numpy

# Database
aiosqlite
sqlalchemy

# Utilities
loguru
python-dotenv

# MT5 connectivity — install ONE depending on your OS:
#   Windows : pip install -r requirements-windows.txt
#   Mac/Linux: pip install mt5linux  (requires Wine + MT5 — see README)
mt5linux
