# Core async framework
fastapi
uvicorn[standard]
websockets

# Data / indicators
pandas
numpy

# Database
aiosqlite
sqlalchemy

# Utilities
loguru
python-dotenv

# MetaTrader5 is Windows-only.
# On Windows run: pip install -r requirements-windows.txt
# The bot handles a missing MT5 package gracefully at runtime.
